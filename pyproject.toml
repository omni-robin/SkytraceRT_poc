[project]
name = "skytracert-poc"
version = "0.0.1"
description = "Raw IQ -> controller band occupancy PoC"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26",
  "torch>=2.2",
]

[tool.ruff]
line-length = 100

[tool.uv]
